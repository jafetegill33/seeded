# seeded
# seeded  seeded is a small CLI tool to generate Debian metapackage source trees from `.seed` files.  ## Commands - `seeded verify file.seed` - `seeded print-deps file.seed` - `seeded render file.seed -o outdir` - `seeded build file.seed -o outdir`
